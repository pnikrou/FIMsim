# ── Core GUI framework ─────────────────────────────────────────────────────────
PyQt6>=6.4.0

# ── Plotting (maps, raster previews, hydrographs) ──────────────────────────────
matplotlib

# ── Scientific / numerical ─────────────────────────────────────────────────────
numpy
scipy          # DEM nodata fill (distance_transform_edt), TRITON Manning

# ── Data wrangling ─────────────────────────────────────────────────────────────
pandas
openpyxl       # Excel file support (discharge CSV/XLSX import)

# ── Geospatial — vector ────────────────────────────────────────────────────────
geopandas
shapely
pyproj         # CRS transforms (used in DEM, BCI, orchestrate)

# ── Geospatial — raster ────────────────────────────────────────────────────────
rasterio

# ── Hydrology data access ──────────────────────────────────────────────────────
requests       # General HTTP
pynhd          # NHD flowlines + USGS gage lookup (USA)
pygeoogc       # WMS client — used directly for NLCD download (also a pynhd dependency)

# ── NWM retrospective download (NOAA Zarr on S3) ───────────────────────────────
xarray
zarr
s3fs
numcodecs

# ── HEC-RAS mesh generation & HDF5 file I/O ───────────────────────────────────
h5py           # Read/write HEC-RAS HDF5 geometry and results files
gmsh           # Triangular mesh generation for HEC-RAS 2D (Tab 7: Build Mesh)

# ── OWP HAND FIM generation (FIM as a Service) ─────────────────────────────────
fimserve ==0.1.99     # AOI -> HUC8 -> NWM discharge -> OWP HAND flood inundation map

# ── Build tool (only needed to create standalone executables, not to run) ──────
# pyinstaller  # Uncomment and install separately when building an installer
